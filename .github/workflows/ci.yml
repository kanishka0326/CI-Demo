name: CI Pipeline

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Build
        run: echo "Build Successful"

      - name: Test
        run: echo "Testing Successful"

      - name: Package
        run: echo "Packaging Completed"